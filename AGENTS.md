# Robotic Arm 项目协作规则

## 适用范围 / Scope

- 本文件是本项目统一的协作规则与 Codex 指令入口，适用于当前 `Robotic Arm/` 文件夹及其全部子目录。
- 开始工作前先阅读本文件、[readme.md](readme.md)、[readme-en.md](readme-en.md) 和待修改文件，检查已有的未提交修改。
- 以当前工程文件为依据，区分本目录、上级目录中的其他机械臂工程和 Arduino 工程，避免混用路径、参数与硬件配置。
- 只修改当前任务涉及的内容，保留用户已有修改。未经用户要求，不调整 `.git/`、Obsidian 配置或其他工程。

## 每次修改后同步 README / Required README Update

**每次对本项目进行新增、修改、删除或重命名后，必须在完成本次任务、向用户回复前同步更新根目录的中文版 `readme.md` 和英文版 `readme-en.md`，无需用户再次提醒。**

1. 根据实际变更更新对应正文：项目用途、目录与入口、使用方法、依赖、当前状态或待办事项。路径变动时同步修正链接。
2. 同步更新两版 README 顶部的 `最后更新` / `Last updated` 日期，采用 `YYYY-MM-DD` 格式。
3. 在“变更记录 / Change Log”顶部新增一条记录，写明日期、涉及文件、变更内容和验证结果。同一任务中的连续编辑合并为一条，不为每次保存重复记录。
4. 即使正文说明无需调整，也必须记录本次实际变更及检查结果；不能只修改日期来代替同步。
5. 文档、配置、CAD/URDF、固件、MATLAB/Simulink 和报告的修改都适用。仅阅读或分析而未改动文件时无需更新。
6. README 自身的修改在同一条记录中说明，不触发递归更新。自动生成且不保留的临时文件无需逐项记录。
7. 完成前重新读取两版 README，确认描述与最终文件一致、链接有效、验证状态准确；两版的项目信息、日期和变更记录须保持对应，并保留语言切换链接。

上述自动更新由遵循本规则的 AI 助手在修改任务中执行。用户在其他软件中手动保存文件不会触发后台更新；后续接手时，应检查与任务相关的差异并补齐说明，明确区分既有修改与本次修改。

## 项目事实与验证 / Project Facts and Verification

- 固件的 MCU 与工具链以 `2. Software/robot_f103.ioc`、工程配置和实际硬件为准；不得直接套用其他工程的 F407 配置。
- 修改 STM32CubeMX 生成代码时，优先使用 `USER CODE` 区域，并核对 `.ioc` 与源文件的对应关系。
- 修改 D-H 参数或目标位姿时，明确单位、坐标系、角度约定及真正的运行输入；文档修改不等于模型或固件已同步。
- 检查 MATLAB 脚本是否实际加载模型、调用仿真。存在模型或构建产物不代表本次运行成功。
- 根据变更选择适当验证：文档检查内容和链接，代码检查或构建，模型检查或仿真，报告检查或编译。未执行的验证必须写明，禁止把静态检查写成编译、仿真或硬件验证通过。
- 连接或驱动实物前核对硬件与接线；限位未接时不得执行回零、Origin 或 Reset。

## 文档风格与交付 / Documentation and Delivery

- 说明以中文为主，`readme-en.md` 使用英文；保留文件名、标识符和必要英文术语。两版 README 都保持为简洁的项目入口。
- 优先使用指向实际文件的相对链接，避免复制其他工程的绝对路径。
- 只记录已验证事实；计划、已知问题和未验证状态应清楚标出。
- 完成回复说明本次变更、README 同步情况及实际检查结果。
